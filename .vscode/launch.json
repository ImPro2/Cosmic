{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Debug Editor (Debug)",
            "preLaunchTask": "Build All (Debug)",
            "expressions": "native",
            "program": "${workspaceFolder}/bin/Debug-linux-x86_64/Editor/Editor"
        }
    ]
}
